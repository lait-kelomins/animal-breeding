$C = "../Common.ui";

// Animal Breeding Configuration Panel
// Layout: Left column (presets + settings) | Right column (animal table)

Group {
  LayoutMode: Center;

  $C.@Container #mainPanel {
    Anchor: (Width: 1600, Height: 960);

    #Title {
      $C.@Title #titleLabel {
        @Text = "HYTAME CONFIGURATION";
      }
    }

    #Content {
      LayoutMode: Left;
      Padding: (Full: 16);

      // ========== LEFT COLUMN - Preset Manager ==========
      Group #leftColumn {
        LayoutMode: Top;
        Anchor: (Width: 490);

        // Section header
        Label {
          Text: "PRESET MANAGER";
          Style: (FontSize: 14, RenderBold: true, TextColor: #ffaa00);
          Anchor: (Height: 24, Bottom: 2);
        }
        Label {
          Text: "Load, save, and manage configuration presets.";
          Style: (FontSize: 11, TextColor: #666666);
          Anchor: (Height: 16, Bottom: 6);
        }

        // Preset search
        Group {
          LayoutMode: Left;
          Anchor: (Height: 36, Bottom: 8);

          $C.@TextField #presetSearch {
            FlexWeight: 1;
            Anchor: (Height: 36, Right: 4);
            PlaceholderText: "Search presets...";
          }
          $C.@SecondaryTextButton #presetSearchBtn {
            @Anchor = (Width: 90, Height: 36);
            Text: "GO";
          }
        }

        // Scrollable preset list
        Group #presetList {
          LayoutMode: TopScrolling;
          ScrollbarStyle: $C.@DefaultScrollbarStyle;
          FlexWeight: 1;
          Background: $C.@InputBoxBackground;
          Padding: (Full: 6);
        }

        // Add Preset button (fixed below scroll list)
        TextButton #addPresetBtn {
          Anchor: (Height: 36, Top: 6);
          Text: "+ ADD PRESET";
          Style: $C.@SecondaryTextButtonStyle;
        }

        // Preset settings section
        Group #presetSettingsPanel {
          LayoutMode: Top;
          Anchor: (Top: 12, Height: 320);
          Background: $C.@InputBoxBackground;
          Padding: (Full: 16);

          Label {
            Text: "PRESET SETTINGS";
            Style: (FontSize: 14, RenderBold: true, TextColor: #ffaa00);
            Anchor: (Height: 28, Bottom: 8);
          }

          Label {
            Text: "Preset Name";
            Style: (FontSize: 12, TextColor: #888888);
            Anchor: (Bottom: 4);
          }
          Group {
            LayoutMode: Left;
            Anchor: (Height: 34, Bottom: 16);

            $C.@TextField #presetRenameInput {
              FlexWeight: 1;
              Anchor: (Height: 34, Right: 4);
            }
            $C.@SecondaryTextButton #renamePresetBtn {
              @Anchor = (Width: 140, Height: 34);
              Text: "RENAME";
            }
          }

          // Growth enabled toggle
          Group {
            LayoutMode: Left;
            Anchor: (Height: 40, Bottom: 10);

            Label {
              Text: "Baby Growth";
              Style: (FontSize: 13, TextColor: #cccccc);
              FlexWeight: 1;
            }
            $C.@SecondaryTextButton #growthToggleBtn {
              @Anchor = (Width: 90, Height: 34);
              Text: "ON";
            }
          }

          // Default cooldown
          Group {
            LayoutMode: Left;
            Anchor: (Height: 40, Bottom: 10);

            Label {
              Text: "Breed Cooldown (min)";
              Style: (FontSize: 13, TextColor: #cccccc);
              FlexWeight: 1;
            }
            $C.@TextField #cooldownInput {
              Anchor: (Width: 80, Height: 34, Right: 4);
            }
            $C.@SecondaryTextButton #applyCooldownAllBtn {
              @Anchor = (Width: 80, Height: 34);
              Text: "ALL";
            }
          }

          // Default growth time
          Group {
            LayoutMode: Left;
            Anchor: (Height: 40);

            Label {
              Text: "Growth Time (min)";
              Style: (FontSize: 13, TextColor: #cccccc);
              FlexWeight: 1;
            }
            $C.@TextField #growthTimeInput {
              Anchor: (Width: 80, Height: 34, Right: 4);
            }
            $C.@SecondaryTextButton #applyGrowthAllBtn {
              @Anchor = (Width: 80, Height: 34);
              Text: "ALL";
            }
          }
        }

        // Hidden action fields for left column buttons
        $C.@TextField #actionToggleGrowth { Anchor: (Width: 0, Height: 0); }
        $C.@TextField #actionSearchPresets { Anchor: (Width: 0, Height: 0); }
        $C.@TextField #actionRenamePreset { Anchor: (Width: 0, Height: 0); }
        $C.@TextField #actionAddPreset { Anchor: (Width: 0, Height: 0); }
        $C.@TextField #actionApplyGrowthAll { Anchor: (Width: 0, Height: 0); }
        $C.@TextField #actionApplyCooldownAll { Anchor: (Width: 0, Height: 0); }
      }

      // Vertical separator (outer group stretches, inner group is the line)
      Group #separator {
        Anchor: (Width: 28);
        Padding: (Horizontal: 10);
        Group { Background: #4a4a5a; }
      }

      // ========== RIGHT COLUMN - Animal List + Detail ==========
      Group #rightColumn {
        LayoutMode: Top;
        FlexWeight: 1;

        // Tab bar
        Group #tabBar {
          LayoutMode: Left;
          Anchor: (Height: 36, Bottom: 8);

          TextButton #tabListBtn {
            Anchor: (Width: 120, Height: 32, Right: 4);
            Text: "LIST";
            Style: $C.@SecondaryTextButtonStyle;
          }
          TextButton #tabEditBtn {
            Anchor: (Width: 120, Height: 32);
            Text: "EDIT";
            Style: $C.@SecondaryTextButtonStyle;
          }
        }

        // Section header
        Label {
          Text: "ANIMAL LIST";
          Style: (FontSize: 14, RenderBold: true, TextColor: #ffaa00);
          Anchor: (Height: 24, Bottom: 2);
        }
        Label {
          Text: "Click an animal to edit its details. Toggle taming and breeding per animal.";
          Style: (FontSize: 11, TextColor: #666666);
          Anchor: (Height: 16, Bottom: 6);
        }

        // Animal search
        Group {
          LayoutMode: Left;
          Anchor: (Height: 36, Bottom: 8);

          $C.@TextField #animalSearch {
            FlexWeight: 1;
            Anchor: (Height: 36, Right: 4);
            PlaceholderText: "Search animals...";
          }
          $C.@SecondaryTextButton #animalSearchBtn {
            @Anchor = (Width: 90, Height: 36);
            Text: "GO";
          }
        }

        // Main content area: scroll list (left) + detail panel (right)
        Group #animalContent {
          LayoutMode: Left;
          FlexWeight: 1;

          // Left side: table header + scrollable list
          Group #animalListSide {
            LayoutMode: Top;
            FlexWeight: 1;
            Padding: (Right: 8);

            // Table header
            Group #tableHeader {
              LayoutMode: Left;
              Anchor: (Height: 22, Bottom: 4);
              Padding: (Horizontal: 8);

              Label { Anchor: (Width: 40); Text: ""; Style: (FontSize: 11, TextColor: #888888); }
              Label { FlexWeight: 1; Text: "NAME"; Style: (FontSize: 11, TextColor: #888888); }
              Label { Anchor: (Width: 100); Text: "FOOD"; Style: (FontSize: 11, TextColor: #888888, Alignment: Center); }
              Label { Anchor: (Width: 94); Text: "TAME"; Style: (FontSize: 11, TextColor: #888888, Alignment: Center); }
              Label { Anchor: (Width: 90); Text: "BREED"; Style: (FontSize: 11, TextColor: #888888, Alignment: Center); }
            }

            // Scrollable animal list
            Group #animalRows {
              LayoutMode: TopScrolling;
              ScrollbarStyle: $C.@DefaultScrollbarStyle;
              FlexWeight: 1;
              Background: $C.@InputBoxBackground;
              Padding: (Horizontal: 8, Vertical: 6);
            }
          }

          // Right side: detail panel
          Group #detailPanel {
            LayoutMode: Top;
            Anchor: (Width: 400);
            Background: $C.@InputBoxBackground;
            Padding: (Full: 10);

            Label #detailTitle { Text: "Select an animal"; Style: (FontSize: 13, RenderBold: true, TextColor: #ffaa00); Anchor: (Height: 22, Bottom: 8); }

            Label { Text: "Cooldown (min)"; Style: (FontSize: 12, TextColor: #888888); Anchor: (Height: 18, Bottom: 2); }
            $C.@TextField #detailCooldown { Anchor: (Height: 30, Bottom: 8); }

            Label { Text: "Growth (min)"; Style: (FontSize: 12, TextColor: #888888); Anchor: (Height: 18, Bottom: 2); }
            $C.@TextField #detailGrowth { Anchor: (Height: 30, Bottom: 8); }

            // Food picker
            Label { Text: "FOODS"; Style: (FontSize: 12, RenderBold: true, TextColor: #ffaa00); Anchor: (Height: 18, Bottom: 4, Top: 8); }
            $C.@TextField #foodSearch {
              Anchor: (Height: 28, Bottom: 4);
              PlaceholderText: "Search foods...";
            }
            Group #foodList {
              LayoutMode: TopScrolling;
              ScrollbarStyle: $C.@DefaultScrollbarStyle;
              FlexWeight: 1;
              Background: $C.@InputBoxBackground;
              Padding: (Full: 4);
            }
          }
        }

        // Save buttons row
        Group {
          LayoutMode: Left;
          Anchor: (Height: 40, Top: 8);

          $C.@SecondaryTextButton #saveAsPresetBtn {
            @Anchor = (Width: 380, Height: 40, Right: 8);
            Text: "SAVE AS PRESET";
          }
          $C.@TextButton #saveBtn {
            @Anchor = (Height: 40);
            FlexWeight: 1;
            Text: "SAVE AS CURRENT CONFIG";
          }
        }

        // Hidden action fields
        $C.@TextField #actionSave { Anchor: (Width: 0, Height: 0); }
        $C.@TextField #actionSaveAsPreset { Anchor: (Width: 0, Height: 0); }
        $C.@TextField #actionCurrentConfig { Anchor: (Width: 0, Height: 0); }
        $C.@TextField #actionSearchAnimals { Anchor: (Width: 0, Height: 0); }
        $C.@TextField #actionSearchFoods { Anchor: (Width: 0, Height: 0); }
        $C.@TextField #actionSwitchToList { Anchor: (Width: 0, Height: 0); }
        $C.@TextField #actionSwitchToEdit { Anchor: (Width: 0, Height: 0); }
      }
    }
  }

}
